# Alert Rules

Place Netdata alert rule configurations here.

Example: `cpu-load.conf`, `memory-usage.conf`, `container-restarts.conf`

These are loaded by Netdata on startup and override defaults in `/etc/netdata/health.d/`.

See docs/ALERT-RUNBOOK.md for creating and managing alerts.
