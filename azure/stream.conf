# Streaming configuration for parent node
# Children connect here and send metrics

# Accept connections from children using the shared streaming API key
[449c2b8f-8a52-467c-b6e6-2532dfafadc2]
  enabled = yes
  default memory = save
  health enabled by default = auto
  allow from = *
  default history = 3600
  default update every = 1

[*]
  # Accept any other API key (fallback)
  enabled = yes
  default history = 3600
  default update every = 1
