# Streaming configuration for parent node
# Children connect here and send metrics

[14.50.65.10-pterodactyl]
  # Pterodactyl host
  enabled = yes
  default history = 3600
  default update every = 1
  
[104.37.190.203-whmcs]
  # WHMCS host  
  enabled = yes
  default history = 3600
  default update every = 1

[*]
  # Default: accept all children
  enabled = yes
  default history = 3600
  default update every = 1
